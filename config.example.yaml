owner_open_id: ou_xxxxxxxxxxxxxxxx
default_repo: agent
default_runtime: codex

repos:
  agent: /Users/you/Code/feishu-agent-remote
  app: /Users/you/Code/my-app
  infra: /Users/you/Code/my-infra

authorized_open_ids:
  - ou_xxxxxxxxxxxxxxxx

lark_cli_bin: lark-cli
bot_names:
  - your-bot-name

features:
  progress_replies: false
  card_replies: false
  runtime_streaming: false
  multi_workspace: false
  card_update_min_interval_seconds: 5

runtimes:
  codex:
    type: codex
    bin: codex
    profile: null
    sandbox: workspace-write
  claude:
    type: claude
    bin: claude
    permission_mode: acceptEdits

agent_templates:
  reviewer:
    runtime: codex
    description: Review diffs and identify risks.
    prompt: |
      请作为代码审查线上专员工作。优先指出 bug、行为回归、风险和缺失测试。任务：${task}
  implementer:
    runtime: codex
    description: Implement and verify a concrete task.
    prompt: |
      请作为实现型线上专员工作。完成任务并运行必要验证。任务：${task}
  reporter:
    runtime: codex
    description: Summarize progress without modifying files.
    prompt: |
      请总结当前 repo/session 的目标、已完成工作、验证状态、风险和下一步。不要修改文件。任务：${task}

# Multi-workspace example:
#
# default_workspace: personal
# shared_repos:
#   agent: /Users/you/Code/feishu-agent-remote
# workspaces:
#   personal:
#     owner_open_id: ou_personal
#     authorized_open_ids:
#       - ou_personal
#     default_repo: agent
#     lark_cli_args:
#       - --profile
#       - personal
#     features:
#       multi_workspace: true
#   team:
#     owner_open_id: ou_team
#     authorized_open_ids:
#       - ou_team
#     default_repo: agent
#     lark_cli_args:
#       - --profile
#       - team
#     features:
#       multi_workspace: true
